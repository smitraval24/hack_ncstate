Fault Code: FAULT_EXTERNAL_API_LATENCY
Target File: hello/page/views_api.py
Target Function: test_fault_external_api

EXACT BUG (line 45):
        r = requests.get(f"{mock_api_base_url}/data", timeout=3)

EXACT FIX — replace that line with:
        r = requests.get(f"{mock_api_base_url}/data", timeout=10)

That is the ONLY change. Do not add retry loops, backoff, circuit
breakers, new imports, or any other code. Change timeout=3 to
timeout=10 and nothing else.

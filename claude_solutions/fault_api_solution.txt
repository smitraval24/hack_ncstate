Fault Code: FAULT_EXTERNAL_API_LATENCY
Target File: hello/page/views_api.py
Target Function: test_fault_external_api

Bug Pattern: The HTTP request timeout is too short for the upstream
API's expected response time, causing frequent timeout exceptions.

What "fixed" looks like: The timeout value is large enough that the
upstream API can respond. The fix is changing the timeout value only —
nothing else in the function needs to change.

Scope: ONE line change. Do not add retry loops, backoff, circuit
breakers, or new imports.

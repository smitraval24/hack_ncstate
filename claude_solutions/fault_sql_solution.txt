Fault Code: FAULT_SQL_INJECTION_TEST
Target File: hello/page/views_sql.py
Target Function: test_fault_run

EXACT BUG (line 30):
        db.session.execute(text("SELECT FROM"))

EXACT FIX — replace that line with:
        db.session.execute(text("SELECT 1"))

That is the ONLY change. Do not add imports, validation, sanitization,
middleware, helper functions, or any other code. Change this one string
and nothing else.

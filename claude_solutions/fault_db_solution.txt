Fault Code: FAULT_DB_TIMEOUT
Target File: hello/page/views_db.py
Target Function: test_fault_db_timeout

Bug Pattern: The function executes a sleep query that takes longer
than the statement timeout, so PostgreSQL always cancels it.

What "fixed" looks like: The sleep duration fits within the statement
timeout so the query completes successfully. The fix is changing the
sleep duration only — nothing else in the function needs to change.

Scope: ONE line change. Do not change statement_timeout, remove
min_delay, or restructure error handling.
